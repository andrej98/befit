package org.acme;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeWorkoutPlanResourceIT extends WorkoutPlanResourceTest {

    // Execute the same tests but in native mode.
}