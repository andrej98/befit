package org.acme;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeRecordResourceIT extends RecordResourceTest {

    // Execute the same tests but in native mode.
}